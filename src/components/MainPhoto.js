import React from "react";
// import TargetingBox from "./TargetingBox";

let MainPhoto = (props) => {
	return (
		<div>
			<img src={props.image} alt="main"></img>
		</div>
	);
};

export default MainPhoto;
